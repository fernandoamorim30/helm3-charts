apiVersion: v1
kind: Service
metadata:
  name: {{ include "facchin.fullname" . }}
  labels:
    {{- include "facchin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "facchin.selectorLabels" . | nindent 4 }}
