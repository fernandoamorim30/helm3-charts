apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "facchin.fullname" . }}-test-connection"
  labels:
    {{- include "facchin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "facchin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
